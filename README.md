# React + Vite

This is about Animal Zoo website, which will fetch data from express Apis

## Prerequisite

Run `npm install` in the root directory

## Frontend

To start frontend, run `npm run dev` in the terminal

## Backend

To start express server, run `npm run start:server` in the terminal

Now try accessing the application at `http://localhost:5173`
